<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Viền ngôi sao rỗng -->
    <item android:id="@android:id/background" android:drawable="@drawable/heartback"/>

    <!-- Sao nửa đầy -->
    <item android:id="@android:id/secondaryProgress" android:drawable="@drawable/hearthalf"/>

    <!-- Sao đầy -->
    <item android:id="@android:id/progress" android:drawable="@drawable/heart"/>
</layer-list>
