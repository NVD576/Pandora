<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Nền trắng -->
    <corners android:radius="12dp"/> <!-- Bo góc mềm -->
    <stroke android:color="@android:color/black" android:width="2dp"/> <!-- Viền đen khi chọn -->
    <padding android:left="16dp" android:right="16dp" android:top="12dp" android:bottom="12dp"/>
</shape>
